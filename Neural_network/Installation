Датасет: https://drive.google.com/drive/folders/18wXs_qK26TDlkGbtN9IJ12vL514P-SW9?usp=sharing


Код на Python представляет собой обучение двух различных моделей нейронных сетей (EfficientNet и VGG16) на изображениях с целью классификации, основываясь на метках из обучающего и тестового наборов данных. Код использует TensorFlow и Keras для построения и обучения моделей, а также библиотеки NumPy, Pandas и Matplotlib для обработки данных и визуализации.
Основные функции и структура кода:

1. Загрузка данных:
   - Загружаются обучающий и тестовый наборы данных из CSV файлов.
   - Собираются пути к изображениям с помощью glob.

2. Исследование данных:
   - Проверяются отсутствующие значения в наборах данных.
   - Анализируется распределение меток в обучающем наборе данных с помощью круговой диаграммы (pie chart) с помощью Plotly.

3. Отображение случайных изображений:
   - Функция для отображения случайных изображений из обучающего набора данных вместе с соответствующими метками.

4. Предобработка изображений:
   - Изображения изменяются до размера 160x160 пикселей и подготавливаются для обучения.
   - Метки преобразуются в формате one-hot с помощью to_categorical.

5. Построение модели:
   - Создается модель на основе архитектуры EfficientNetB7 без верхнего слоя, адаптированная для новой задачи классификации.
   - Замораживаются слои предобученной модели и добавляются собственные полносвязные слои для классификации.

6. Обучение модели:
   - Компилируется и обучается модель EfficientNet на обучающих данных в течение указанного числа эпох.
   - Аналогично с моделью VGG16.
